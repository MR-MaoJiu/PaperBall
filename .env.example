# 环境变量配置示例
# 复制此文件为 .env 并根据需要修改配置

# 是否使用模拟 API (true/false)
# 设置为 true 时，应用将使用内置的模拟数据，无需后端服务器
# 设置为 false 时，应用将连接到真实的后端 API
VITE_USE_MOCK_API=true

# 后端 API 地址 (仅在 VITE_USE_MOCK_API=false 时使用)
# 开发环境默认: http://localhost:3001/api
# 生产环境: 你的后端部署地址
VITE_API_BASE_URL=http://localhost:3001/api

# 其他配置
# 应用标题
VITE_APP_TITLE=纸团社交

# 地图 API 密钥 (如果需要)
# VITE_MAP_API_KEY=your_map_api_key_here